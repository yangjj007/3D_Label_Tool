# ============================================
# 3D Label Tool 环境变量配置文件模板
# ============================================
# 
# 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 根据实际情况修改配置
# 3. 不要将 .env 文件提交到 Git 仓库
# ============================================

# -----------------
# 前端API地址配置
# -----------------
# 本地开发（前后端在同一台机器）
VITE_API_BASE_URL=http://localhost:10000/api

# 或远程服务器访问（前端在本地，后端在服务器）
# VITE_API_BASE_URL=http://10.26.2.3:10000/api

# 或使用域名
# VITE_API_BASE_URL=https://api.yourdomain.com/api

# -----------------
# Vite基础路径
# -----------------
VITE_APP_BASE_URL=/

# -----------------
# 文件分块大小（可选）
# -----------------
# 默认 10MB (10 * 1024 * 1024)
# VITE_CHUNK_SIZE=10485760

# ============================================
# 注意事项：
# ============================================
# 1. ⚠️ 不要使用 0.0.0.0 作为API地址
#    - 0.0.0.0 是服务器监听地址，不是客户端访问地址
#    - 本地开发请使用: http://localhost:10000/api
#    - 局域网访问请使用: http://实际IP:10000/api
#
# 2. 后端端口配置
#    - 后端端口通过 PORT 环境变量配置
#    - 启动命令：PORT=10000 npm run dev:full
#
# 3. 修改配置后需要重启服务
#    - Ctrl+C 停止当前服务
#    - 重新运行启动命令

