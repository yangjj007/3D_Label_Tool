# 后端服务器配置
# 后端API服务器端口
PORT=30005

# 前端配置（VITE_ 开头的变量会被 Vite 自动加载）
# 后端 API 地址（开发环境）
VITE_API_BASE_URL=http://localhost:30005/api

# 大文件分块传输大小（单位：MB）
VITE_CHUNK_SIZE=10485760

# 应用部署的基础路径
VITE_APP_BASE_URL=/
