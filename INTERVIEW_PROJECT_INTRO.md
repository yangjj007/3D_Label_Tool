# 3D 模型多模态数据处理项目面试介绍

## 简短发言稿

我这个项目可以概括为一条面向 3D 资产库的多模态语义数据处理管线。它不是只做 3D 模型展示，而是把 GLB/GLTF/OBJ 这类原始模型，经过模型解析、部件/材质粒度定位、PartField 分割、多视角渲染、VLM 语义生成、质量过滤和人工可视化复核，转成可检索、可训练、可追溯的 3D-2D-Text 多模态数据。

项目里我重点解决了三个问题：第一，3D 数据本身不适合直接喂给视觉语言模型，所以我设计了多视角截图和高亮目标区域的方式，把 3D 结构转成 VLM 更稳定理解的 2D 视觉证据；第二，标注不是只生成一段文本，而是绑定到具体 mesh、material 或 segment 上，并写回模型文件和服务端元数据，保证语义和几何对象能对齐；第三，为了保证数据质量，我加入了提示词库、并发重试、无效标签过滤、几何复杂度指标筛选、截图证据留存和可视化人工编辑，形成自动生成加质量门控的闭环。

如果放到 3D 模型多模态数据处理岗位里，我觉得这个项目最能体现的是：我理解 3D 数据、2D 视觉、多模态语义和工程管线之间的连接方式，也能把一个模型能力包装成可批量运行、可恢复、可审核、可落盘的数据生产系统。

## 项目亮点

- **多模态数据管线**：将 3D 模型解析为几何对象，再通过多视角渲染生成 2D 图像证据，最后调用 VLM 生成语义标签，实现 3D 几何、2D 图像和文本标签的对齐。
- **细粒度语义绑定**：标签不是存在一个外部表里，而是绑定到 mesh/material/segment，并支持写回 GLB/GLTF 的 `userData/extras` 或 OBJ 的材质注释，降低后续数据漂移风险。
- **证据链可追溯**：围绕 `segId -> 多视角截图 -> prompt -> label` 保存数据，后续审核能看到模型、分割块、视角证据和生成文本之间的关系。
- **自动化批处理能力**：通过 Puppeteer 连接 Headless Chrome，自动打开前端、触发批量标注、监控进度和生成报告；VLM 调用层支持并发控制、重试、指数退避和代理。
- **数据质量门控**：不仅看模型输出，还结合材质数量、VVD/VFC/VSC 体素复杂度指标、无效关键词过滤、截图证据留存和人工复核，保证标注数据可筛选、可解释。
- **工程化服务设计**：后端统一管理模型上传、分块续传、元数据、标签、分割任务和健康检查；PartField 推理服务支持 GPU 预加载，避免每次分割重复加载模型。
- **可维护的模块拆分**：前端把渲染、标注、VLM、过滤、文件持久化拆成独立模块，例如 `vlmService.js`、`filterService.js`、`modelComplexity.js`、`renderModel.js`、`serverApi.js`。

## 参考工业数据飞轮后的升级表达

我参考了 Label Studio、CVAT、FiftyOne、Cleanlab、DVC、lakeFS、MLflow、Flyte、OpenScene、3D-LLM 等开源项目的成熟设计后，会把这个项目进一步表述为一个 **3D 多模态数据飞轮原型**。它的闭环不是“生成一次标签就结束”，而是：

```text
原始 3D 资产
  -> 几何解析/分割
  -> 多视角渲染
  -> VLM 预标注
  -> 人工审核/修正
  -> 质量门禁
  -> 数据集版本发布
  -> 训练/评估
  -> 失败样本和人工反馈回流
```

这个表达更贴近工业级数据系统，因为它强调三个核心能力：

- **数据血缘**：每个标签都能追溯到原始模型、segment id、多视角截图、prompt 版本、VLM 模型版本、人工审核记录和质量指标。
- **人机协同**：模型只产出 `prediction`，人工修正后才形成 `annotation`，复核通过后才进入 `reviewed/released` 数据集。
- **质量门禁**：候选数据必须经过完整性、3D-2D 对齐、无效标签、几何复杂度、重复样本、分布漂移和抽检通过率检查，才能进入训练集。
- **开放语义资产**：参考 OpenScene、OpenMask3D、ConceptFusion 这类思路，不只保存最终类别名，还可以保存 segment 级 embedding、多视角证据和文本查询能力，让后续新增类别时不用重标全量数据。

如果面试官问“你怎么把这个项目从 demo 升级成工业数据飞轮”，我会回答：我会先补统一 manifest 和状态机，把 `raw -> segmented -> pre_labeled -> reviewed -> qa_passed -> released -> feedback` 固化下来；再补 `prediction / annotation / review` 三层数据结构；最后引入主动选样，把低置信、长尾类别、模型失败、人工多次修改和质量异常样本自动回流到下一轮标注。

如果面试官问“你对 3D 多模态数据飞轮的理解是什么”，我会说：我会把它拆成三层，**几何可信、语义开放、语言可交互**。几何可信靠 mesh/point cloud/depth/pose/segment 对齐来保证；语义开放靠多视角 VLM、CLIP/OpenSeg 类特征和可查询 embedding 来扩展类别；语言可交互则把部件、材质、空间关系、affordance 转成 caption、QA、referring expression 和指令数据。

## 深挖细节设计

### 1. 整体数据流怎么设计？

原始 3D 文件先进入后端统一模型目录，服务端生成 `meta.json` 记录状态和元数据；前端 Three.js 加载模型后提取 mesh/material 列表，用户或自动化脚本选择目标部件；系统按预设相机位生成多视角截图，把这些截图和提示词一起发给 VLM；VLM 返回的文本标签再写入模型对象、标签文件和服务端元数据。最终一个样本不只是“模型 + 文本”，而是包含原始模型、分割信息、视角截图、语义标签、质量指标和时间戳的完整数据包。

### 2. 为什么要做多视角渲染，而不是直接让模型看一个截图？

3D 模型的语义往往依赖空间结构，一个视角容易出现遮挡或误判。我的做法是预设主视图、俯视图、侧视图、轴测图等相机位，并在选中目标材质/部件时做高亮截图，让 VLM 同时看到局部目标和整体上下文。这样可以把 3D 空间问题转成更稳定的多图像理解问题，也方便后续审核时追溯模型当时看到的视觉证据。

### 3. 如何保证 3D 对象和文本标签对齐？

我避免只用文件名或顺序来绑定标签，而是优先用 mesh 的 `uuid/name`、material 名称或 segment id 作为锚点。GLB/GLTF 会写入对象的 `userData.semanticLabel` 和材质的 `userData.label`，OBJ 会在 `usemtl` 行追加标签注释。服务端标签结构也按 segment/material 保存，避免批量处理后标签和几何对象错位。

### 4. 如何保证 3D 分割块和 2D 截图中的高亮区域对齐？

分割流程会产出每个三角面的 `faceLabels`，前端渲染时把 segment id 映射成顶点色，并在点击或选中某个 segment 时把其他区域压暗、目标区域高亮。多视角截图使用固定的相机预设，所以同一个 segment 可以在不同视角下生成一致的视觉证据。这里最关键的是保证 PartField 预处理后的网格、前端加载的网格和 `faceLabels` 的面序一致；如果生产化，我会增加模型 hash、mesh 版本、face count 校验和截图元数据，避免因为拓扑变化导致标签错位。

### 5. 材质粒度和分割粒度有什么区别？

材质粒度来自资产制作流程，适合快速定位和编辑，但它不一定等于真实语义部件，比如多个部件可能共用一个材质。分割粒度更接近几何和语义部件，适合做部件级标签、局部问答和训练数据构建，但会引入过分割、欠分割和边界不准的问题。所以我的设计是两套粒度兼容：材质粒度保证可用性和编辑效率，segment 粒度用于更细的多模态数据构建。

### 6. 怎么保证数据质量？

质量控制分三层。第一层是生成前控制：提示词库统一约束输出格式，支持随机/加权选择，减少单一提示词带来的偏差。第二层是生成后过滤：过滤空结果、错误结果和类似 `Unknown Object` 的无效标签，并记录成功/失败数量。第三层是数据集级质量门控：用材质数量、顶点/面片数量、体素占用和 VVD/VFC/VSC 复杂度指标筛选模型，剔除过简单、过碎或几何质量异常的样本。最后还有可视化编辑入口，人工可以直接查看截图和模型部件并修正标签。

### 6.0 模型过滤与统计界面解决什么问题？

这部分我会讲成数据入库前的质量门禁，而不是普通的筛选弹窗。3D 数据质量有两类问题：一类是语义标签是否准确，另一类是模型本身是否值得进入训练集。截图里的“模型过滤与统计”就是处理第二类问题：先对已打标模型计算几何和材质指标，再用阈值把过简单、过碎、拓扑异常或复杂度分布不合理的模型拦下来。

具体指标分三组。第一组是材质个数过滤，它不是严格语义质量，但能快速发现异常资产，比如只有一个材质的模型可能语义部件不可分，材质数量过多的模型可能过碎或贴图拆分异常。第二组是基础几何统计，比如顶点数、面片数、网格数量、占用体素数和表面体素数，用来判断模型是否过粗、过密或计算成本过高。第三组是体素复杂度指标：`VVD = 顶点数 / 占用体素数`，衡量单位空间里的顶点密度；`VFC = 面片数 / 占用体素数`，衡量单位空间里的面片复杂度；`VSC = 表面体素数 / 占用体素数`，近似反映表面积/体积比，能区分规则块状物和细碎复杂结构。

这里的体素分辨率参数，本质上是在控制几何统计的尺度。如果分辨率太低，细节会被抹平；如果太高，计算成本会上升且对噪声更敏感。所以界面把体素分辨率、并发量、指标开关和阈值都做成可配置，让不同数据集可以调整质量门禁，而不是固定一套死规则。统计分析页会展示总文件数、有指标数据的文件数、每个指标的最小值、最大值、均值、标准差、中位数和密度分布图，用来帮助我反向设定阈值。面试时可以说：我不是凭感觉过滤数据，而是先看分布，再设 batch-level quality gate。

### 6.1 提示词库怎么做工程化设计？

我会把提示词库讲成一个面向 3D 多模态标注的数据生产契约，而不是几段自然语言模板。项目里的提示词已经包含了主流 prompt 设计里的几类关键元素：角色设定，例如 industrial design expert 或 daily necessities operations specialist；任务边界，例如只分析黄色边框内区域、忽略背景和 UI；输出约束，例如单段、单句、必须以 It/This is/The 开头；负向约束，例如不要描述未选中区域、不要提截图和高亮；few-shot 示例，例如 trigger、keycap、tire 三类交互和非交互样例。这些设计能降低 VLM 输出发散，让同一类 3D segment 更稳定地产生部件、材质、颜色、交互方式和功能结果。

如果要把它升级成生产级提示词工程，我会拆成八个模块：`role` 定义模型身份，`visual_scope` 约束只看目标边界，`task` 指定要识别部件和功能，`interaction_schema` 规定 press/rotate/slide/flip/non-interactive 等动作集合，`physical_schema` 规定 material/color/texture/marking 等物理字段，`output_contract` 规定单句或 JSON schema，`negative_rules` 禁止越界描述和无证据推断，`examples` 用少量稳定样例控制风格。这样 prompt 就像一个可维护的配置对象，可以通过 `prompt_id/version/weight` 管理，而不是散落在代码里的长字符串。

这里我还会主动指出一个工程判断：原始提示词一方面要求“精确材料、颜色、力、行程、角度”，另一方面又要求“不要外推、不要引用外部知识”。这两个要求在真实 3D 截图里可能冲突，因为力值、行程和材料牌号很多时候不能仅凭视觉确认。我的解决方式是把输出拆成两层：对外展示仍然是一句自然语言，但内部先生成结构化结果，例如 `component_name`、`feature_name`、`interaction_type`、`operation`、`travel_or_angle`、`material`、`color`、`surface_marking`、`evidence_level`、`confidence`、`needs_review`。如果参数不可见，就不让模型硬编，而是标成 `not_observable` 或进入人工复核队列。这样既满足面试题里“描述交互属性”的目标，也能避免把幻觉数据混进训练集。

面试时可以这样讲：我没有把 prompt 当成一次性文案，而是把它当成 3D 数据管线里的可版本化算子。每次 VLM 标注都会记录 `prompt_id`、`prompt_version`、模型名、视角组合、输出字段和人工修改记录；提示词库支持随机或加权选择，本质上可以做 A/B 测试，比较不同 prompt 的无效标签率、人工修改率、跨视角一致性和审核通过率。表现好的 prompt 提升权重，容易诱导幻觉或格式漂移的 prompt 降权或下线。这个思路和主流 prompt engineering 方法是一致的：[OpenAI prompt engineering best practices](https://help.openai.com/en/articles/6654000-best-practices-for-prompt-engineering-with-the-openai-api) 强调指令前置、分隔符、具体格式、examples 和低温度抽取；[Google Gemini prompt design strategies](https://ai.google.dev/gemini-api/docs/prompting-strategies) 强调清晰具体指令、few-shot 示例、上下文补充和迭代优化；[Anthropic prompt engineering overview](https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview) 强调先定义成功标准和 eval，再迭代 prompt。放到本项目里，就是用工程指标管理 prompt，而不是靠感觉调 prompt。

### 7. 如果面试官问“模型审核是不是也用同一个模型判？”

我会这样回答：当前项目没有把“同一个 VLM 自己生成、自己裁判”作为唯一质量依据。生成模型负责产出候选语义，基础审查先用本地规则做风险分层，比如空标签、`Unknown Object`、标签过短、描述截图/高亮/UI、包含不可见的力值/行程/角度等硬编码参数，都会被标成 `fail` 或 `needs_review` 并写回 segment 的 `review` 字段。质量门控还会结合几何指标、无效关键词过滤、截图证据和人工复核。原因是同模型自审容易有一致性偏差，看起来判断通过但其实只是重复了同一套错误。

如果扩展到生产级审核，我会采用交叉评审策略：生成模型和审核模型分离，或至少使用不同 prompt、不同温度和不同视角证据进行二次判别。当前实现里，批量审查会优先读取 VLM 配置，把已保存的 segment 多视角截图和原始标签一起发给 reviewer VLM，让它只判断“标签是否被可见证据支持”，并要求返回结构化 JSON。没有配置 VLM 时，系统自动降级为本地规则审查。审核输出不直接覆盖标签，而是写成结构化结果：`status`、`score`、`reason`、`issues`、`suggestedLabel`、`reviewedAt`、`reviewerModel`。这样能把模型审核变成质量分层，而不是简单的“模型说对就是对”。

现在界面上已经能体现这个闭环：左侧已打标列表可以触发批量审查，结果会写入 `labels/info.json` 的每个 segment；右侧点击某个分割掩码时，除了原始标签，也会显示审查状态、置信分和审查理由。这样面试官追问“你怎么知道生成标签合理”时，我可以回答：第一层是本地规则和格式审查，第二层是基于多视角截图证据的 VLM 审查，第三层是高风险样本进入人工复核队列。由于 VLM 审查会把 segment 截图发送到当前配置的模型服务，生产环境里可以通过私有化部署、脱敏数据集或白名单模型服务来控制数据外发风险。

### 8. PartField 分割在项目里承担什么角色？

材质粒度不一定等于真实语义部件，比如一个椅子腿和椅背可能共用材质。为了解决更细粒度的数据处理问题，项目接入了 PartField 分割流程，把模型切成更接近语义部件的 segment，再对 segment 进行选中、高亮、截图和标注。服务端还设计了预加载推理服务：启动时把模型权重放到 GPU，后续通过 HTTP 请求执行分割；如果预加载服务不可用，会 fallback 到子进程方式，保证功能可用。

### 8.1 分割算法和簇数选择怎么设计？

我会把这里讲成“PartField 特征场 + 聚类后处理”的设计。PartField 负责为三角面或采样点生成部件级特征，真正落到可标注 segment 时，还需要一个聚类算法把相似特征合并成部件。项目没有只绑定一种算法，而是在批量打标配置里暴露了 Agglomerative、K-Means 和 HDBSCAN 三种选择，本质上是为了适配不同 3D 资产的拓扑质量、输入形态和自动化程度。

默认选择凝聚聚类是因为它最适合 mesh 部件分割。它可以引入 face adjacency，把空间上相邻、特征上相似的三角面优先合并，减少“同一个扶手被切碎”或“隔得很远但特征相似的面被合并”的问题。同时凝聚聚类会形成一棵层次树，根节点是完整物体，叶子是细粒度三角面，中间层就是不同颗粒度的部件候选，所以后续既可以自动切，也可以让人工沿着层次树细分。对于拓扑比较碎、连通性差的网格，项目里采用 face centroid 的 KNN/MST 邻接方式补 pseudo edge，让层次聚类在脏 mesh 上也尽量稳定。

K-Means 是更轻量的 fallback。它不依赖面邻接矩阵，只看 PartField 特征空间，因此适合点云输入、Gaussian splats，或者 mesh 连通性很差、不希望做重网格预处理的情况。它的缺点是必须给定 K，且空间连续性约束弱，容易把相似但不相邻的面归到一起；所以在本项目里我会把它定位成“拓扑不可信时的特征聚类备选”，而不是默认方案。批量模式下 K-Means 的自动 K 可以用材质数量作为弱先验，因为很多工业资产的材质划分虽然不等于语义部件，但能提供一个粗粒度结构参考。

HDBSCAN 是面向未知簇数的密度聚类方案。它不需要提前指定部件数量，可以从特征密度里自动发现簇，并把离群点标成噪声，适合处理部件数量不确定、长尾形态多、人工先验不足的数据。代码里还做了两个工程优化：第一，PartField 特征维度较高时先用 PCA 降到较低维度，提高 HDBSCAN 的邻域搜索效率；第二，对 HDBSCAN 输出的噪声点，用原始高维特征做 KNN 回填，避免最终 face label 里出现大量无法渲染或无法标注的 `-1` 区域。它的定位不是替代所有算法，而是作为“全自动探索模式”，用于发现异常形态和不规则分割。

簇数选择也不是简单手填一个数字。凝聚聚类默认用树形图最大间距法，也就是看层次合并距离里哪一次跳跃最大，把它当成自然部件边界；这个方法快、稳定，适合批量数据。轮廓系数法会遍历 `K=2..auto_max_clusters`，比较类内紧密度和类间分离度，优点是更数据驱动，缺点是计算更重，适合抽样验证或对关键批次做更精细选择。手动指定 K 则保留给有业务先验的场景，比如明确要把办公椅拆成坐垫、靠背、扶手、底座和脚轮等固定层级。面试时我会强调：自动 K 解决规模化，手动 K 解决可控性，silhouette 解决质量验证。

这张批量打标配置面板也体现了工程化取舍。最大并行数控制整体模型处理和 VLM 请求吞吐，GPU 并发数单独控制同时截图的材质或 segment 数量，因为 WebGL 截图、GPU 读回和浏览器渲染比普通请求更容易过载。截图视角可以在主视、俯视、侧视、轴测之间选择，目的是控制多视角证据的成本和覆盖率。对大批量数据，默认用少量稳定视角；对细小部件或遮挡严重的部件，再增加轴测或局部视角。这里不是单纯给用户调参数，而是把算法质量、视觉证据成本和系统稳定性都放进同一个可配置入口。

如果要继续生产化，我会再加两类保护：第一，保存每次分割的 `method`、`num_clusters/requested_clusters`、`auto_method`、`auto_max_clusters`、实际 segment 数和耗时，让分割结果可复现、可比较；第二，把算法枚举做成前后端统一契约和能力探测，避免 UI 支持某种方法但 fallback 脚本不可用的接口漂移。这类细节能体现我不是只会调用算法，而是在考虑算法落到数据生产系统里的可靠性。

### 9. 批量处理如何保证稳定性？

VLM 客户端做了并发信号量控制、最大重试、指数退避和随机抖动，避免请求过载。自动化脚本通过 Puppeteer 连接远程调试端口的 Chrome，先检查前端、后端和 WebGL 状态，再触发 `window.startBatchLabeling`，处理过程中监控总数、已处理数、耗时、ETA 和卡死状态。文件上传也做了分块上传、断点检查、合并和取消，适合大模型文件。

### 10. 数据如何持久化和追溯？

服务端以模型 id 为目录组织数据：原始模型、`meta.json`、分割结果、标签 `info.json`、overview 截图和 segment 截图都放在同一模型目录下。`meta.json` 维护状态流转，例如 raw、segmenting、segmented、labeled、filtered，并记录过滤指标和时间戳。这样一个样本从上传、分割、标注到过滤都能追溯，后续做数据集清洗或训练数据回放时不会丢上下文。

如果继续增强，我会把数据 schema 固化得更严格：补充原始模型 hash、VLM 模型名、prompt id/version、viewKeys、相机参数、渲染尺寸、PartField checkpoint、分割配置、人工编辑历史和审核状态。这样能把“标注结果”升级成真正可复现实验、可回滚、可抽检的数据资产。

### 11. 这个项目里最容易影响数据集可靠性的风险是什么？

第一类风险是对齐风险，比如分割后的 face label 和前端网格面序不一致，解决方式是增加拓扑校验、面数校验和版本记录。第二类风险是 schema 漂移，比如早期接口按 files/materials 组织，后续主流程按 models/segments 组织，如果不统一会导致漏样本或状态错乱。我的治理思路是收敛到统一数据 schema，保留兼容层但给 deprecated 标记，并增加端到端校验：上传一个模型后自动验证分割、截图、标签、元数据和状态流转是否完整。

### 12. 这个项目体现了哪些工程设计能力？

我觉得主要体现在四点：一是把 3D、多视角图像和文本语义拆成明确的数据结构，而不是只做页面功能；二是把 VLM 能力封装成可配置、可重试、可并发的服务客户端；三是把大文件、GPU 推理、Headless 渲染、标签持久化这些不稳定环节都做了工程兜底；四是质量体系不是后补的，而是从提示词、截图证据、标签写回、指标过滤和人工复核贯穿整条链路。

### 13. 如果面试官问“工业数据飞轮里最关键的 schema 是什么？”

我会说不是单纯的标签 JSON，而是统一样本 manifest。里面至少要包含 `sample_id`、`asset_uri`、`segment_id`、`view_keys`、`annotation_uri`、`prompt_version`、`prelabel_model`、`review_status`、`quality_metrics`、`dataset_version`。这样训练、审核、回滚和问题排查都能基于同一份数据契约，而不是靠文件夹命名约定。

### 14. 如果面试官问“如何让数据越用越好？”

我会把它讲成主动学习/错误回流机制：训练或线上使用后，把低置信、预测错误、人工频繁修改、分割异常、长尾类别和跨视角不一致的样本打上高优先级，进入下一轮标注队列。这样每一轮模型评估都会反过来驱动数据生产，而不是盲目扩大数据量。

### 15. 如果面试官问“为什么保留中间资产，而不是只保存最终标签？”

我会回答：3D 多模态数据的价值不只在最终标签，而在可复用证据链。多视角截图、相机位、segment mask、几何指标、prompt、模型版本和 per-segment embedding 都是后续重审、扩类、训练和错误分析的基础。比如新增“可交互部件”类别时，如果我只存了文本标签，就要重新标注；如果我存了 segment 级视觉证据和 embedding，就可以先用文本查询和模型筛选候选，再让人工复核。

## 面试官追问速答

- **多视图会不会增加成本？** 会，所以我把视角做成可配置。默认用能覆盖空间结构的少量标准视角；对细小部件再追加局部放大或更多视角，而不是所有样本无差别堆图。
- **VVD/VFC/VSC 代表语义质量吗？** 不完全代表。它们是几何复杂度和可处理性指标，用来筛掉过简单、过碎或异常模型；语义质量还要结合标签完整率、跨视角一致性、无效标签率和人工抽检。
- **如果 VLM 幻觉怎么办？** 通过提示词约束、无效关键词过滤、截图证据留存和人工复核降低风险。生产化会加入审核模型、置信度、标签 schema 校验和需要复核的队列。
- **提示词怎么评估上线？** 我不会只看单条输出是否顺眼，而是按批次统计无效标签率、格式通过率、人工修改率、跨视角一致性和审核通过率。每个 prompt 都有版本和权重，表现好的提升权重，诱导幻觉或格式漂移的降权。
- **如果截图看不出力值、行程或材料牌号怎么办？** 不让模型硬编。把这类字段标为 `not_observable` 或低置信，必要时交给人工复核；可见信息和推断信息分层保存，避免把视觉幻觉当成训练真值。
- **怎么评价分割结果？** 看 segment 是否对应可命名部件、边界是否贴合、是否过碎或跨语义合并。必要时把分割质量作为独立字段，不让低质量分割直接进入训练集。
- **为什么默认用凝聚聚类？** 因为 mesh 分割不只看特征相似，还要看三角面邻接。凝聚聚类能利用 face adjacency 形成层次部件树，更符合“部件是空间连续区域”的假设。
- **什么时候用 K-Means？** 当输入是点云，或者 mesh 连通性很差、邻接矩阵不可信时，用 K-Means 作为不依赖拓扑的特征聚类 fallback，但要接受空间连续性弱和需要指定 K 的代价。
- **HDBSCAN 的价值是什么？** 它适合不知道部件数量的开放集场景，能自动发现高密度簇并标出离群点；项目里会用 PCA 提速，再用 KNN 把噪声点回填成可用 segment。
- **自动簇数怎么定？** 默认用凝聚树形图最大间距法找自然切分点；对关键批次可以用轮廓系数扫描 `K=2..N` 做更细评估；有明确业务先验时保留手动 K。
- **为什么并行数和 GPU 并发数分开？** VLM 请求和 WebGL 截图的瓶颈不同。普通并行数管吞吐，GPU 并发数管同时截图和读回的压力，拆开后可以在速度和稳定性之间调平衡。
- **这个项目和普通 3D 编辑器最大的区别？** 普通编辑器主要关注交互和展示；这个项目关注的是把 3D 资产转成结构化多模态样本，核心是数据对齐、批量自动化、质量门控和可追溯。
- **为什么说它是数据飞轮，不只是标注工具？** 因为它把模型预标注、人工修正、质量门禁、数据版本、训练评估和失败样本回流串起来。工具解决一次标注，飞轮解决数据持续变好。
- **3D 标注最难的点是什么？** 不是起一个类别名，而是跨视角和跨模态一致性：同一个 segment 在 3D 几何、多视角截图和文本描述里必须指向同一个对象。

## 数据质量深挖追问

- **你们怎么定义一条高质量 3D 多模态样本？** 我会定义为四个对齐：3D 几何对象可定位、2D 视角证据清晰、文本语义准确且结构化、元数据可追溯。也就是不只看标签“像不像”，还要看 `segId/meshId`、截图、prompt、模型版本和审核状态是否能完整串起来。
- **标签生成有没有统一 schema？** 面试时可以说：基础版是自然语言段落，生产化会收敛到结构化 schema，例如部件名称、材质、颜色、功能、空间关系、可交互属性、置信度和拒答原因。这样便于做自动校验、搜索和训练集统计。
- **如何发现 VLM 幻觉？** 我会结合三类信号：跨视角是否都支持这个结论，标签是否出现与类别无关的物体或过度推断，是否命中了无效词和低置信模式。比如椅子脚轮被说成“摄像头”，就可以通过类别先验、局部截图和人工复核拦下来。
- **跨视角一致性怎么做？** 对同一 segment 保留 main/top/side/axial 多张截图，要求标签描述不能只依赖单一视角。如果一个部件在侧视图像扶手、俯视图却落在靠背区域，就要检查 segment 选择、相机位或分割结果是否错位。
- **分割质量如何进入数据质量判断？** 分割质量要单独记录，不应该默认分割出来就可用。判断标准包括 segment 是否可命名、边界是否贴合、是否过分割、是否把多个语义部件合在一起，以及 segment 数量是否和物体复杂度大致匹配。
- **几何复杂度指标能不能替代人工审核？** 不能。VVD/VFC/VSC 更像“几何可处理性”和“异常样本筛选”指标，能帮我们发现过碎、过密、过简单或体素化异常的模型；语义是否准确还要结合截图、标签完整率、跨视角一致性和人工抽检。
- **如何设计人工抽检策略？** 不需要所有样本等比例抽检。可以优先抽检低置信、分割块过多/过少、标签过短、出现无效关键词、几何指标异常、模型类别长尾的样本。这样人工成本集中在最可能出问题的数据上。
- **怎么防止同一模型重复进入训练集？** 需要记录原始文件 hash、模型 id、来源和处理版本。即使文件名不同，只要 hash 或几何签名高度相似，就要合并或标记为重复，避免训练/验证集泄漏。
- **如果 prompt 更新了，旧数据怎么办？** 旧数据不能静默覆盖。应该记录 prompt id/version、VLM 模型名、生成时间和审核状态；新 prompt 生成的标签作为新版本，必要时保留历史版本用于对比和回滚。
- **如何判断一批数据可以入库？** 我会设一组批次级指标：标签完成率、失败率、无效标签率、人工抽检通过率、平均 segment 数、几何指标分布、重复率、跨视角截图缺失率。只有这些指标达标，才把 batch 状态从 labeled 推到 filtered/accepted。
- **审核模型是否会带来二次误判？** 会，所以审核模型更适合做风险分层，而不是直接裁决真伪。它可以输出“通过、需人工复核、拒绝”的建议，并给出原因；关键样本仍要靠规则证据和人工复核兜底。
- **如果线上批处理失败，怎么避免污染数据集？** 每个模型有状态机，比如 raw、segmenting、segmented、labeled、filtered。失败样本不能伪装成成功样本，应该保留错误原因、失败阶段和可重试状态，批次统计时单独计算。

## 管线案例：一把办公椅

以一个 `office_chair.glb` 为例，我会这样向面试官展示整条管线：

1. **模型入库**：用户上传办公椅 GLB，后端分块接收并合并，生成模型目录和 `meta.json`，记录文件大小、上传时间、状态和后续处理指标。
2. **几何解析**：前端用 Three.js 加载模型，解析出 mesh/material；如果模型本身材质比较清晰，可能能拿到“坐垫、靠背、扶手、底座、轮子”等材质或对象名称。
3. **部件分割**：如果材质粒度不够，比如坐垫和靠背共用黑色皮革材质，就触发 PartField 分割。默认使用带 face adjacency 的凝聚聚类，并用树形图最大间距法自动选择簇数，得到 `faceLabels` 后把椅子切成靠背、坐垫、左右扶手、气压杆、五星脚、脚轮等 segment；如果网格连通性很差，就切到 K-Means 或 HDBSCAN 做 fallback。
4. **3D-2D 对齐渲染**：选中“脚轮”这个 segment 时，前端根据 face label 给目标三角面高亮，其他区域压暗；然后从主视图、侧视图、俯视图、轴测图截图，形成这个 segment 的多视角视觉证据。
5. **VLM 语义生成**：把多张截图和统一 prompt 发给 VLM，要求输出部件名称、材质、颜色、功能和空间关系。比如脚轮的候选标签可以是：“椅子底部的黑色双轮脚轮，连接在五星底座末端，用于支撑和移动办公椅。”
6. **生成后校验**：如果模型输出“unknown object”或把脚轮说成“按钮/摄像头”，会被关键词、类别先验或人工复核拦下；如果标签太泛，比如只说“black part”，也会被判为信息量不足。
7. **质量指标筛选**：计算整把椅子的材质数量、顶点数、面片数、VVD/VFC/VSC。如果模型极度简化导致部件不可分，或者面片异常复杂导致渲染和分割不稳定，就不会直接进入高质量数据集。
8. **人工可视化复核**：审核者打开工具，点击“脚轮”segment，能同时看到 3D 高亮区域、已有标签和多视角截图。如果发现“扶手”和“靠背”被合并，就可以修正标签或把该 segment 标为低质量。
9. **数据落盘**：最终保存的不只是文本，而是 `office_chair.glb`、`meta.json`、分割结果、每个 segment 的截图、`labels/info.json` 和过滤指标。后续训练或排查时，可以回放“这个标签是基于哪几张图、哪个 prompt、哪个 segment 生成的”。
10. **入库结果**：这条样本最终变成 3D-2D-Text 三元数据：3D 上知道脚轮对应哪些面，2D 上有多个视角截图，Text 上有部件级语义描述。它可以用于 3D 资产检索、部件级问答、自动标注训练或多模态对齐数据构建。

一个可以直接讲的小结是：这把办公椅从原始 GLB 进入系统后，不是简单生成一句“这是一把椅子”，而是被拆成可定位的部件，每个部件都有多视角证据、语义标签、质量状态和可追溯元数据。这样产出的数据才更接近多模态训练真正需要的样本。

## 可主动补充的一句话

这个项目最有价值的地方，是它把“3D 模型看懂”拆成了一个可落地的数据生产问题：先把 3D 几何转成稳定的多视角视觉证据，再把语义结果绑定回可追溯的 3D 对象，最后用规则、指标和人工复核把数据质量管起来。
