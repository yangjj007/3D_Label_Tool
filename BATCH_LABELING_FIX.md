# 批量打标脚本问题修复总结

## 问题描述

原始错误：
```
❌ [浏览器错误] Error creating WebGL context.
❌ 应用加载失败: Waiting failed: 30000ms exceeded
```

核心问题：**WebGL 上下文创建失败**，导致 Three.js 3D 应用无法运行。

## 主要问题

1. **WebGL 不可用**
   - 无头浏览器环境中没有 GPU 支持
   - Chrome 启动参数配置不正确
   - 缺少软件渲染后端（SwiftShader）

2. **端口配置不一致**
   - Chrome 启动脚本使用固定端口 29999
   - 批量打标脚本使用端口 9999
   - 导致 Chrome 导航到错误的地址

3. **日志输出不足**
   - 无法诊断具体问题
   - 缺少 WebGL 状态检查
   - 缺少浏览器启动参数验证

## 修复内容

### 1. 增强日志输出

#### 修改文件：`automation/batch-labeling.js`

**添加的功能：**

- ✅ 详细的 Chrome 连接信息
- ✅ 浏览器版本和用户代理输出
- ✅ **WebGL 支持检查**（关键）
  - WebGL 供应商、渲染器、版本
  - 最大纹理尺寸、视口尺寸
  - 明确提示 WebGL 是否可用
- ✅ Three.js 加载检查
- ✅ 前端 API 可用性检查
- ✅ 完整的浏览器控制台日志捕获
- ✅ 所有 API 请求/响应日志
- ✅ 页面错误和请求失败详细信息

**新增日志示例：**

```
🔍 检查 WebGL 支持...
✅ WebGL 可用
   供应商: Google Inc. (Google)
   渲染器: ANGLE (Google, Vulkan 1.x.x (SwiftShader))
   版本: WebGL 1.0 (OpenGL ES 2.0 Chromium)
   着色语言版本: WebGL GLSL ES 1.0 (OpenGL ES GLSL ES 1.0 Chromium)
   最大纹理尺寸: 16384
   最大视口尺寸: [16384,16384]

🔍 检查 Three.js...
✅ Three.js 已加载 (版本: r134)

🔍 检查前端 API...
   API 可用性:
     - startBatchLabeling: ✅
     - stopBatchLabeling: ✅
     - getBatchStatus: ✅
```

### 2. 修复端口配置问题

#### 修改文件：`start_chrome_swiftshader.sh` 和 `start_chrome_xvfb.sh`

**修改内容：**

```bash
# 从环境变量读取配置，支持自定义端口
CHROME_DEBUG_PORT="${CHROME_DEBUG_PORT:-30000}"
SERVER_PORT="${SERVER_PORT:-9999}"

# Chrome 启动时使用变量
--remote-debugging-port=$CHROME_DEBUG_PORT
http://localhost:$SERVER_PORT
```

**优点：**
- ✅ 端口配置统一管理
- ✅ 支持环境变量覆盖
- ✅ 自动从 `start-batch-labeling.sh` 继承端口配置

#### 修改文件：`start-batch-labeling.sh`

**修改内容：**

```bash
# 在启动 Chrome 时传递正确的环境变量
SERVER_PORT=$server_port CHROME_DEBUG_PORT=$chrome_debug_port bash start_chrome_swiftshader.sh

# 添加 Chrome 调试端口验证
if curl -s "http://localhost:$chrome_debug_port/json/version" > /dev/null; then
    log_success "Chrome 调试端口可访问"
fi
```

### 3. 优化 Chrome 启动参数

#### 修改文件：`start_chrome_swiftshader.sh`

**添加的参数：**

```bash
--enable-logging=stderr    # 启用日志输出
--v=1                      # 详细日志级别
```

**修复的路径：**
- 日志从 `/tmp/chrome.log` 改为 `logs/chrome.log`（项目目录下）
- 用户数据目录包含端口号，避免冲突：`/tmp/chrome-batch-labeling-$CHROME_DEBUG_PORT`

### 4. 创建诊断工具

#### 新文件：`check-webgl.js`

**功能：**
- 连接到正在运行的 Chrome 实例
- 检查 WebGL 1.0 和 2.0 是否可用
- 显示详细的渲染器信息
- 判断是否使用 SwiftShader 或 GPU
- 给出性能建议

**使用方法：**
```bash
node check-webgl.js
```

**输出示例：**
```
╔════════════════════════════════════════════════════════════╗
║                      WebGL 1.0                            ║
╠════════════════════════════════════════════════════════════╣
║ 状态: ✅ 可用                                            ║
╠════════════════════════════════════════════════════════════╣
║ 供应商:       Google Inc. (Google)                       ║
║ 渲染器:       ANGLE (Google, Vulkan 1.x.x (SwiftShader))║
║ 版本:         WebGL 1.0                                   ║
╚════════════════════════════════════════════════════════════╝

✅ 结论: WebGL 可用，应用应该可以正常运行！

⚠️  注意: 正在使用 SwiftShader (软件渲染)
   性能: 比 GPU 慢 10-100 倍
   建议: 降低并发数到 4-8
```

#### 新文件：`diagnose.sh`

**功能：**
- 一键检查所有服务状态
- 检查端口占用情况
- 验证 Chrome 进程和启动参数
- 测试服务连接
- 检查文件目录
- 运行 WebGL 检查
- 生成诊断摘要和建议

**使用方法：**
```bash
bash diagnose.sh
```

**输出示例：**
```
╔════════════════════════════════════════════════════════════╗
║              批量打标系统 - 诊断工具                      ║
╚════════════════════════════════════════════════════════════╝

[1/6] 检查依赖...
  ✅ node: /usr/bin/node
  ✅ google-chrome: /usr/bin/google-chrome
  ✅ curl: /usr/bin/curl

[2/6] 检查端口占用...
  ✅ 端口 9999 (前端服务): 被 PID 12345 (node) 占用
  ✅ 端口 10000 (后端服务): 被 PID 12346 (node) 占用
  ✅ 端口 30000 (Chrome调试): 被 PID 12347 (chrome) 占用

[3/6] 检查 Chrome 进程...
  ✅ Chrome 进程运行中
     PID: 12347
     关键参数:
       ✅ 使用 SwiftShader 软件渲染
       ✅ WebGL 未被禁用

[4/6] 测试服务连接...
  ✅ 前端服务: http://localhost:9999
  ✅ 后端健康检查: http://localhost:10000/api/health
  ✅ Chrome调试接口: http://localhost:30000/json/version

╔════════════════════════════════════════════════════════════╗
║                        诊断摘要                            ║
╠════════════════════════════════════════════════════════════╣
║ ✅ 所有服务运行正常！                                    ║
╚════════════════════════════════════════════════════════════╝

✅ 系统就绪！可以开始批量打标
```

#### 新文件：`WEBGL_TROUBLESHOOTING.md`

**内容：**
- 问题描述和症状
- 快速诊断方法
- 常见问题和解决方案
- 完整的启动流程
- 性能优化建议
- 日志说明
- 常用命令参考

## 使用指南

### 快速开始

```bash
# 1. 一键诊断（推荐首次运行）
bash diagnose.sh

# 2. 如果一切正常，直接运行批量打标
bash start-batch-labeling.sh

# 3. 如果有问题，查看故障排除指南
cat WEBGL_TROUBLESHOOTING.md
```

### 问题排查流程

```bash
# 步骤 1: 运行诊断
bash diagnose.sh

# 步骤 2: 如果 Chrome 未启动或 WebGL 不可用
bash start_chrome_swiftshader.sh

# 步骤 3: 验证 WebGL
node check-webgl.js

# 步骤 4: 如果 WebGL 可用，运行批量打标
bash start-batch-labeling.sh

# 步骤 5: 如果还有问题，查看日志
tail -f logs/chrome.log
tail -f logs/batch-labeling-*.log
```

### 常见问题解决

#### 问题 1: WebGL 不可用

```bash
# 解决方案：使用 SwiftShader 软件渲染
pkill -f "chrome.*remote-debugging-port"
bash start_chrome_swiftshader.sh
node check-webgl.js
```

#### 问题 2: 端口冲突

```bash
# 查看占用端口的进程
lsof -i:9999
lsof -i:10000
lsof -i:30000

# 杀死占用进程或修改端口配置
```

#### 问题 3: Chrome 启动失败

```bash
# 查看 Chrome 日志
tail -50 logs/chrome.log

# 检查 Chrome 是否安装
google-chrome --version

# 手动测试启动
bash start_chrome_swiftshader.sh
```

## 性能建议

### SwiftShader 模式（CPU 软件渲染）

适用于：无 GPU 的服务器环境

```bash
export CONCURRENCY=4      # 并发数
export VIEW_KEYS=axial    # 只使用一个视图
bash start-batch-labeling.sh
```

**预期性能：**
- 处理速度：5-30 秒/文件
- CPU 使用率：80-100%
- 建议并发数：4-8

### Xvfb + GPU 模式

适用于：有 GPU 的服务器环境

```bash
export CONCURRENCY=16     # 并发数
export VIEW_KEYS=axial,sagittal,coronal  # 多个视图
bash start-batch-labeling.sh
```

**预期性能：**
- 处理速度：1-5 秒/文件
- GPU 使用率：高
- 建议并发数：16-32

## 文件清单

### 修改的文件

1. `automation/batch-labeling.js` - 增强日志和诊断
2. `start_chrome_swiftshader.sh` - 修复端口和路径
3. `start_chrome_xvfb.sh` - 修复端口和路径
4. `start-batch-labeling.sh` - 改进 Chrome 启动流程

### 新增的文件

1. `check-webgl.js` - WebGL 诊断工具
2. `diagnose.sh` - 系统诊断脚本
3. `WEBGL_TROUBLESHOOTING.md` - 详细故障排除指南
4. `BATCH_LABELING_FIX.md` - 本文档

## 技术细节

### WebGL 检查逻辑

```javascript
// 在页面中创建测试 Canvas
const canvas = document.createElement('canvas');
const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');

if (gl) {
  // 获取渲染器信息
  const debugInfo = gl.getExtension('WEBGL_debug_renderer_info');
  const renderer = gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL);
  
  // 判断是否使用 SwiftShader
  if (renderer.includes('SwiftShader')) {
    console.log('使用 CPU 软件渲染');
  }
}
```

### Chrome 启动参数说明

**SwiftShader 模式（推荐）：**
```bash
--headless=new                  # 新版无头模式
--use-gl=swiftshader           # 使用 SwiftShader
--enable-unsafe-swiftshader    # 启用所有 SwiftShader 特性
--disable-gpu                  # 禁用 GPU（因为要用软件渲染）
```

**Xvfb + GPU 模式：**
```bash
--display=:99                      # 使用 Xvfb 虚拟显示
--disable-software-rasterizer      # 禁用软件光栅化
--enable-gpu-rasterization         # 启用 GPU 光栅化
--ignore-gpu-blocklist             # 忽略 GPU 黑名单
```

**禁止使用的参数：**
```bash
--disable-webgl       # 会完全禁用 WebGL！
--disable-3d-apis     # 会禁用所有 3D API
```

## 测试验证

### 测试步骤

1. **清理环境：**
```bash
pkill -f "chrome.*remote-debugging-port"
pkill -f "node.*server/index.js"
pkill -f "node.*vite"
```

2. **运行诊断：**
```bash
bash diagnose.sh
```

3. **启动服务：**
```bash
bash start-batch-labeling.sh
```

4. **验证 WebGL：**
```bash
node check-webgl.js
```

5. **检查日志：**
```bash
tail -f logs/chrome.log &
tail -f logs/batch-labeling-*.log
```

### 预期结果

- ✅ 所有服务正常启动
- ✅ WebGL 可用（SwiftShader 或 GPU）
- ✅ 前端 API 正确暴露
- ✅ 批量打标正常运行
- ✅ 详细的日志输出

## 后续改进建议

1. **添加自动降级机制**
   - 如果 GPU 模式失败，自动切换到 SwiftShader
   - 如果 SwiftShader 失败，尝试其他渲染后端

2. **性能监控**
   - 实时显示 CPU/GPU 使用率
   - 预估剩余时间更准确

3. **错误恢复**
   - 单个文件失败不影响整体流程
   - 自动重试失败的文件

4. **界面改进**
   - Web UI 显示实时进度
   - 支持暂停/恢复
   - 支持查看当前处理的文件

## 联系方式

如果遇到问题，请提供：
1. `bash diagnose.sh` 的完整输出
2. `node check-webgl.js` 的输出
3. `logs/chrome.log` 和 `logs/batch-labeling-*.log`
4. 系统信息：`uname -a` 和 `google-chrome --version`

